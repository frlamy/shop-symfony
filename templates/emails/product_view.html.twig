<h1>Visite du produit :
	{{ product.name }}</h1>

<h2>Détail du produit :</h2>
<p>
	{% if user %}
		L'utilisateur qui regarde actuellement cette page est :
		{{ user.fullName }}</p>
{% endif %}
<ul>
	<li>Prix :
		{{product.price | amount}}</li>
	<li>Slug:
		{{product.slug}}</li>
</ul>
